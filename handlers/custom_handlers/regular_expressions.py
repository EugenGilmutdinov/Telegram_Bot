# def show_hotels(res):
# for hotel in suggestions["results"]:
#     print(hotel)
    # id_pattern = r"(?<='id': ).+?(?=,)"
    # name_pattern = r"(?<='name': ').+?(?=')"
    # starRating_pattern = r"(?<='starRating': ).+?(?=,)"
    # streetAddress_pattern = r"(?<='streetAddress': ').+?(?=')"
    # extendedAddress_pattern = r"(?<='extendedAddress': ').+?(?=')" old pattern
    # extendedAddress_pattern = r"(?<='extendedAddress': ')[^'].+?(?=')"
    # locality_pattern = r"(?<='locality': ').+?(?=')"
    # region_pattern = r"(?<='region': ')[^'].+?(?=')"
    # countryName_pattern = r"(?<='countryName': ').+?(?=')"
    # rating_pattern = r"(?<='rating': ').+?(?=')"
    # exactCurrent_pattern = r"(?<='exactCurrent': )\d+.\d+"
    # url_photo_pattern = r"(?<='srpDesktop': ').+?(?=')"
    # distance_pattern = r"(?<='Центр города', 'distance': ').+?(?=')"
    # try:
        # id = re.search(id_pattern, str(hotel))
        # name = re.search(name_pattern, str(hotel))
        # starRating = re.search(starRating_pattern, str(hotel))
        # streetAddress = re.search(streetAddress_pattern, str(hotel))
        # extendedAddress = re.search(extendedAddress_pattern, str(hotel))
        # locality = re.search(locality_pattern, str(hotel))
        # region = re.search(region_pattern, str(hotel))
        # countryName = re.search(countryName_pattern, str(hotel))
        # rating = re.search(rating_pattern, str(hotel))
        # exactCurrent = re.search(exactCurrent_pattern, str(hotel))
        # url_photo = re.search(url_photo_pattern, str(hotel))
        # distance = re.search(distance_pattern, str(hotel))